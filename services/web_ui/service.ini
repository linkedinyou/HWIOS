[service]
port = 80
listen = 0.0.0.0
ssl = false

[general]
lang = nl-NL
cache = memcached://127.0.0.1:11211/
time_zone = Europe/Amsterdam
threadpool = 10
template = start
debug = True
secret_key = pkivyqv97u7+l#o8tdrokc4p!5el96f*exe(%)y4s-j^0)i((0
session = db #db for session persistance, cache for in-memory sessions only

[system]
unprivileged_user = hwios  ;Username to use when dropping down from root (same username as opensim runs under)
unprivileged_group = users    ;Group to use when dropping down from root (probably users, or the username)
